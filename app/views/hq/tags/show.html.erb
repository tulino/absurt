<p>
  <strong>Title:</strong>
  <%= @tag.name %>
</p>


<%= link_to 'Edit', edit_hq_tag_path(@tag) %> |
<%= link_to 'Destroy', hq_tag_path(@tag),
                method: :delete,
                data: { confirm: 'Are you sure?' } %>

<%= link_to 'Back', root_path %>
