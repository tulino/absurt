<p>
  <strong>Title:</strong>
  <%= @brand.name %>
</p>
<% @products.each do |product| %>
    <a href="<%= product_path(product) %>"><%= product.name %> </a><br>
<% end %><br><br><br>



<%= link_to 'Edit', edit_hq_brand_path(@brand) %> |

<%= link_to 'Back', root_path %>
